# Home Control

Home Control is a service that manages automations for [Zigbee2MQTT](https://zigbee2mqtt.io).

## Requirements

* NodeJS 24
    * This may work with other versions, but only the major version specified in `.nvmrc` is officially supported.
