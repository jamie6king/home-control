li.device
    - const deviceUrl = `/dashboard/devices/${device.id}`

    a(href=deviceUrl)
        h3= device.name

    if device.type === "light"
        button(onclick=`toggleDevice(event, "${device.id}")`) Toggle
